# Keep real credentials in .env. Do not commit .env.
DATABASE_URL=postgresql://user:password@host:5432/database

# Optional. Use require for managed databases that enforce TLS.
# PGSSLMODE=require

# Optional local port for the showcase API and static server.
# PORT=8787
